# Pasos para ejecutar los escenarios:

### 1. Instalar las librerias
  `$ npm install`
### 2. Ejecutar las pruebas
 `$ npx cypress open`

Los escenarios estan especificados dentro del archivo 
posts.cy.js